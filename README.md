```
                ()
          ()
              ()
Bubbles.js  ()
```

This is a bubble: `()`

Bubbles.js counts bubbles.

You use it like this:

```js
count = await bubbles

    ()  ()
 ()  ()
        ()

console.log(`There are ${count} bubbles.`)
```
